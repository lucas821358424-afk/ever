<UserControl x:Class="EverInspection.Client.Views.FormSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <DockPanel Margin="20">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="280" Height="40" FontSize="16" Text="{Binding Keyword, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="搜索" Width="100" Height="40" Margin="10,0,0,0" Command="{Binding SearchCommand}"/>
            <Button Content="新建填写" Width="140" Height="40" Margin="10,0,0,0" Command="{Binding NewFormCommand}"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Templates}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="All" FontSize="15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="表单名称" Binding="{Binding TemplateName}" Width="2*"/>
                <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="*"/>
                <DataGridTextColumn Header="更新时间" Binding="{Binding UpdatedAt}" Width="*"/>
                <DataGridTextColumn Header="未上传数量" Binding="{Binding PendingUploadCount}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
