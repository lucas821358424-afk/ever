<UserControl x:Class="EverInspection.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="账号" VerticalAlignment="Center" FontSize="16" />
        <TextBox Grid.Column="1" Text="{Binding UserId, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="40" FontSize="16"/>

        <TextBlock Grid.Row="1" Text="密码" VerticalAlignment="Center" FontSize="16" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,5" Height="40" FontSize="16"/>

        <CheckBox Grid.Row="2" Grid.ColumnSpan="2" Content="在线模式" IsChecked="{Binding IsOnline}" FontSize="16" Margin="0,10" />

        <Button Grid.Row="3" Grid.ColumnSpan="2" Content="登录" Height="46" FontSize="18" Command="{Binding LoginCommand}" />
    </Grid>
</UserControl>
