# Windows 平板点检客户端系统设计方案（WPF/.NET Framework 4.8）

## 1. 文档目的
本文档基于已确认的业务需求，给出一套可落地的技术方案与实施蓝图，用于指导“模板驱动点检客户端”开发、联调与验收。

---

## 2. 建设目标与范围

### 2.1 建设目标
- 在 Windows 10 平板上提供可离线运行的点检客户端。
- 基于模板动态渲染多类型点检表，不为每种表单单独开发页面。
- 支持离线填写、本地保存、在线同步、权限控制、超规格报警。
- 界面视觉接近原 Excel（表格布局、边框、对齐、字段结构）。

### 2.2 范围边界
- **本期范围**：账号登录、模板同步、动态表单、校验、SQLite 本地库、同步、权限、历史查询。
- **预留扩展**：电子签名、拍照附件、条码扫描、NFC、多语言、字段类型扩展。

---

## 3. 运行环境与技术栈

### 3.1 环境约束
- 开发环境：Windows 7
- 运行环境：Windows 10（触屏平板，最低分辨率 1280×800）
- 网络环境：企业内网，支持 HTTPS

### 3.2 技术选型
- 语言与框架：C# + WPF + .NET Framework 4.8
- 本地数据库：SQLite（建议 SQLCipher 或同类加密方案）
- 架构模式：MVVM + 分层架构（UI / Application / Domain / Infrastructure）
- 日志：NLog 或 Serilog（滚动文件）
- 通信：HttpClient + TLS（HTTPS）

---

## 4. 总体架构设计

### 4.1 逻辑分层
1. **表现层（WPF）**
   - 登录页、表单选择页、动态填写页、历史记录页、同步页。
   - DataGrid/ItemsControl + DataTemplate 动态渲染。
2. **应用层（UseCase）**
   - 登录、模板更新、权限检查、表单实例创建、草稿保存、提交、同步。
3. **领域层（Domain）**
   - 模板模型、表单实例模型、校验规则、超规格判定、状态机。
4. **基础设施层（Infrastructure）**
   - SQLite 仓储、HTTP API 客户端、缓存、日志、配置、加密服务。

### 4.2 核心模块
- AuthModule：首次在线认证、离线登录、锁定机制。
- TemplateModule：模板与版本管理、差异更新。
- DynamicFormModule：模板驱动 UI 渲染。
- ValidationModule：本地实时校验与超规报警。
- DraftModule：自动保存/草稿管理。
- SyncModule：上传未上传数据 + 下载模板/权限。
- PermissionModule：界面层 + 业务层双重权限校验。
- HistoryModule：按条件查询与只读详情。

---

## 5. 关键业务流程

### 5.1 登录流程
1. 用户输入账号密码。
2. 有网：服务端认证成功后缓存用户、权限、令牌、用户摘要。
3. 无网：仅允许已成功在线登录过的用户离线登录。
4. 连续失败达到阈值触发锁定（可配置）。
5. 登录成功进入“表单选择界面”。

### 5.2 模板更新流程
1. 启动或登录后检测网络。
2. 有网时请求模板版本清单。
3. 下载新增/变更模板及权限数据。
4. 保存模板历史版本，不覆盖旧版本引用。
5. 更新本地索引，供动态渲染与历史回放。

### 5.3 动态填写流程
1. 用户在表单列表中选择表单模板。
2. 客户端根据模板版本生成表头与明细表格。
3. 输入时实时进行合法性、必填、范围校验。
4. 超规格立即高亮并记录状态。
5. 自动保存草稿（定时 + 字段变更触发）。
6. 提交时再执行全量校验，成功后状态转“已提交（待上传）”。

### 5.4 同步流程
1. 网络可用时自动/手动触发同步。
2. 先上传本地“已提交未上传”数据。
3. 上传成功后更新状态为“已上传”。
4. 再拉取模板与权限增量更新。
5. 失败项记录错误信息并可重试。

---

## 6. 动态模板与数据模型设计

### 6.1 模板主结构（建议）
- TemplateId
- TemplateName
- Version
- UpdatedAt
- EnablePanelId（bool）
- EnableMachineType（bool）
- HeaderFields（EQ ID、Panel ID、机种、时间、操作员等）
- Rows（制程、点检项目、单位、Min、Max、是否必填、字段类型等）

### 6.2 表单实例结构（建议）
- FormInstanceId（GUID）
- TemplateId
- TemplateVersion
- OperatorId
- HeaderValues（JSON）
- DetailValues（JSON/子表）
- Status（Draft/Submitted/Uploaded）
- HasOutOfSpec（bool）
- CreatedAt / UpdatedAt / SubmittedAt / UploadedAt

### 6.3 版本一致性策略
- 表单实例创建时绑定 `TemplateId + TemplateVersion`。
- 历史查询按实例绑定版本渲染，不使用最新模板覆盖旧结构。
- 模板删除采用“逻辑下线”，禁止物理删除历史引用版本。

---

## 7. 本地数据库设计（SQLite）

### 7.1 建议表
- `users`：用户缓存与离线登录索引。
- `permissions`：用户表单权限。
- `templates`：模板元数据。
- `template_contents`：模板结构 JSON。
- `form_instances`：表单实例主表。
- `form_instance_items`：明细行（如需结构化查询）。
- `sync_logs`：同步日志与错误原因。
- `app_settings`：本地配置（锁定阈值、超规提交策略、自动同步开关）。

### 7.2 安全建议
- 数据库文件加密（SQLCipher 或操作系统受保护目录 + 二次加密）。
- 密码仅存哈希（PBKDF2/BCrypt），不存明文。
- 令牌加密存储并设置过期策略。

---

## 8. 动态界面实现方案（WPF）

### 8.1 视图布局
- 顶部：用户信息、网络状态、同步状态。
- 左侧/主区：可权限过滤的表单列表（名称、版本、更新时间、未上传数量、搜索）。
- 填写页：
  - 表头区：根据模板条件显示字段。
  - 主体区：固定列结构
    - 制程 | 点检项目 | 单位 | Min | Max | 点检值

### 8.2 Excel 风格还原要点
- DataGrid 样式统一边框线、行高、单元格居中/左对齐策略。
- 制程列支持分组与合并显示（通过分组模板或自定义绘制模拟）。
- 只读列（Min/Max）灰底；可编辑列（点检值）白底。
- 超规单元格红底 + 警示图标/文本。
- 必填字段显示红色星标。

### 8.3 触屏交互优化
- 控件最小点击区域 >= 36px。
- 常用按钮（保存、提交、同步）大尺寸且固定位置。
- 输入框聚焦时弹出数字键盘（可选）。

---

## 9. 校验与报警规则

### 9.1 本地实时校验
- 数值字段仅允许合法数字（含负号/小数点按需求开关）。
- 非法字符实时拦截并提示。
- 必填未填：禁止提交。
- 范围校验：`value < Min || value > Max` => 超规格。

### 9.2 超规格处理
- UI 即时高亮（红色背景）。
- 可配置弹窗提醒开关。
- 超规格标识写入实例明细。
- 配置项 `AllowSubmitOutOfSpec` 控制是否允许提交。

---

## 10. 离线与同步机制

### 10.1 状态定义
- Draft：草稿，可编辑/删除。
- Submitted：已提交，待上传。
- Uploaded：已上传，默认只读。

### 10.2 自动保存
- 触发策略：字段变更后延迟保存（如 1~2 秒防抖）+ 定时保存（如 30 秒）。
- 保存耗时目标 <= 0.5 秒。
- 异常保存失败时写日志并提示。

### 10.3 同步策略
- 自动同步：网络恢复事件触发。
- 手动同步：用户点击按钮。
- 幂等：上传接口带 `FormInstanceId` 防重。
- 重试：指数退避 + 手动强制重试。
- 进度提示：已处理条数/总条数。

---

## 11. 权限控制设计

### 11.1 权限粒度
- FormVisible（可见）
- FormFill（可填写）
- HistoryEdit（可修改历史）
- DraftDelete（可删除草稿）

### 11.2 双重校验
- UI层：无权限不显示按钮/入口。
- 业务层：UseCase 执行前再次检查权限，防绕过。

### 11.3 失效处理
- 同步发现权限收回时，立即刷新界面。
- 关键权限失效（如登录态失效）强制重新登录。

---

## 12. 历史记录与查询
- 查询条件：日期范围、表单类型、上传状态。
- 列表展示：表单名、版本、操作员、填写时间、状态。
- 详情页：按原模板版本只读渲染。
- 已上传记录默认不可编辑。

---

## 13. 性能目标与实现建议

### 13.1 目标
- 启动时间 <= 5 秒
- 表单打开 <= 1 秒
- 保存 <= 0.5 秒
- 支持 >= 100 模板本地存储与加载
- 长时间运行无明显内存增长

### 13.2 技术手段
- 模板解析缓存（内存 + 本地索引）。
- UI 虚拟化（VirtualizingStackPanel）。
- 异步 IO（数据库、网络均异步队列化）。
- 大对象生命周期管理，避免事件泄漏。

---

## 14. 安全与合规
- 全部服务端通信使用 HTTPS。
- 口令不落盘明文，采用哈希存储。
- 本地敏感数据加密（令牌、数据库文件）。
- 日志脱敏（账号、令牌、设备标识）。
- 不在客户端硬编码密钥。

---

## 15. 异常处理与可观测性
- 断网：顶部明显提示“离线模式”。
- 同步失败：提示错误原因并提供重试入口。
- 数据库异常：记录日志 + 用户可感知提示。
- 崩溃恢复：启动后检测未完成草稿并恢复。
- 统一日志等级：Info/Warn/Error/Fatal。

---

## 16. 扩展性预留设计
- 字段类型可扩展：数值、文本、下拉、布尔、日期、图片、签名等。
- 控件工厂模式：按字段类型映射控件模板。
- 附件子系统预留：本地文件索引 + 同步任务队列。
- 国际化预留：资源字典 + 多语言键值。

---

## 17. 验收清单（对应需求）

### 17.1 核心功能验收
- [ ] 动态模板驱动生成表单（无硬编码页面）
- [ ] 离线填写可用，网络恢复可同步
- [ ] 超规格即时报警与提交策略生效
- [ ] 权限控制覆盖列表、填写、历史、删除
- [ ] 历史数据按模板版本一致渲染
- [ ] 界面结构与 Excel 高度一致

### 17.2 非功能验收
- [ ] Windows 10 平板稳定运行
- [ ] 启动/打开/保存性能达标
- [ ] 100+ 模板加载无明显卡顿
- [ ] 长时间运行无内存持续增长

---

## 18. 实施计划建议（里程碑）
1. **M1（2周）**：基础框架、登录、SQLite、配置中心、日志。
2. **M2（2周）**：模板管理、动态表单渲染、基础校验。
3. **M3（2周）**：草稿/提交/历史、超规格策略。
4. **M4（2周）**：同步、权限双校验、离线增强。
5. **M5（1~2周）**：性能优化、稳定性测试、UAT 验收。

---

## 19. 风险与应对
- **Excel 视觉还原复杂**：优先定义样式规范与可接受偏差范围。
- **离线数据一致性风险**：强制版本绑定 + 幂等上传。
- **权限变更时效风险**：增加同步触发点（登录后、定时、手动）。
- **现场网络波动**：断点续传、失败重试、离线可用优先。

